# Routes

TanStack Router generates `src/routeTree.gen.ts` from these files during development and production builds. Keep the root outlet in `__root.tsx`; it renders each page. This project uses client-side React rendering with a static Vite entry point.
